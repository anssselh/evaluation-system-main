# v0 sandbox internal files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts
.npmrc
.snowflake/
.v0-trash/
.vercel/
next.user-config.*

# Environment variables
.env*.local

# Common ignores
node_modules/
.next/
.DS_Store

# Windows reserved device name (phantom git entry)
NUL

# Secrets — never commit
.env

# Use yarn.lock only (not both lockfiles)
package-lock.json

# Course material / local files
support de bolockchain.pdf